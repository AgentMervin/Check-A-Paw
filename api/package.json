{
  "name": "check-a-paw-api",
  "version": "1.0.0",
  "description": "Foundry IQ grounding proxy for Check-A-Paw (keeps the API key off the browser).",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0"
  }
}
